# qd-bloom

Encrypted audit dashboard. `index.html` holds AES-256-GCM ciphertext;
the passphrase is shared out of band. Redacted export: scores, techniques
and coverage only — no transcripts or scenario text.

`legacy-alignment-run.html` is an earlier, unencrypted run covering only
benign alignment behaviors (honesty / oversight / epistemics).
